import 'package:flutter/material.dart';

class LandingPageTablet extends StatelessWidget {
	LandingPageTablet({Key key}) : super(key: key);

	@override
	Widget build(BuildContext context) {
		return Material(color: Colors.transparent,);
	}
}