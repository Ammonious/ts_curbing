import 'package:flutter/material.dart';

class SectionTwoDesktop extends StatelessWidget {
	SectionTwoDesktop({Key key}) : super(key: key);

	@override
	Widget build(BuildContext context) {
		return Material(color: Colors.transparent,);
	}
}