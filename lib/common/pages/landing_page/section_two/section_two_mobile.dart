import 'package:flutter/material.dart';

class SectionTwoMobile extends StatelessWidget {
	SectionTwoMobile({Key key}) : super(key: key);

	@override
	Widget build(BuildContext context) {
		return Material(color: Colors.transparent,);
	}
}
