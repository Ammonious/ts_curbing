import 'package:flutter/material.dart';

class SectionTwoTablet extends StatelessWidget {
	SectionTwoTablet({Key key}) : super(key: key);

	@override
	Widget build(BuildContext context) {
		return Material(color: Colors.transparent,);
	}
}