import 'package:flutter/material.dart';

class AdminHostMobile extends StatelessWidget {
	AdminHostMobile({Key key}) : super(key: key);

	@override
	Widget build(BuildContext context) {
		return Material(color: Colors.transparent,);
	}
}
