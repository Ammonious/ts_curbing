import 'package:flutter/material.dart';

class AdminHostTablet extends StatelessWidget {
	AdminHostTablet({Key key}) : super(key: key);

	@override
	Widget build(BuildContext context) {
		return Material(color: Colors.transparent,);
	}
}