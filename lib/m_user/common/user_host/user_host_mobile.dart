import 'package:flutter/material.dart';

class UserHostMobile extends StatelessWidget {
	UserHostMobile({Key key}) : super(key: key);

	@override
	Widget build(BuildContext context) {
		return Material(color: Colors.transparent,);
	}
}
