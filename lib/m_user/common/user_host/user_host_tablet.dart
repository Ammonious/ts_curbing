import 'package:flutter/material.dart';

class UserHostTablet extends StatelessWidget {
	UserHostTablet({Key key}) : super(key: key);

	@override
	Widget build(BuildContext context) {
		return Material(color: Colors.transparent,);
	}
}