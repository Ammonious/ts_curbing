import 'package:flutter/material.dart';

class UserHostDesktop extends StatelessWidget {
	UserHostDesktop({Key key}) : super(key: key);

	@override
	Widget build(BuildContext context) {
		return Material(color: Colors.transparent,);
	}
}